import Autores from "./Autor";
import Categorias from "./Categoria";
import Emprestimo from "./Emprestimo";
import EmprestimoLivros from "./EmprestimoLivro";
import Livros from "./Livro";
import Usuario from "./Usuario";

(async() => {
  // await Usuario.sync({ force: true});
  // await Emprestimo.sync({ force: true });
  // await Categorias.sync({ force: true });
  // await Autores.sync({ force: true });
  // await Livros.sync({ force: true });
  // await EmprestimoLivros.sync({ force: true });
})();